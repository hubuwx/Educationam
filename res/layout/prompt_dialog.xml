<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="180dp"
    android:orientation="vertical"
    android:id="@+id/popup_element"
    >

    <Button
        android:id="@+id/btnDownloadOnly"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnViewOnly"
        android:layout_alignBottom="@+id/btnViewOnly"
        android:layout_marginLeft="54dp"
        android:layout_toRightOf="@+id/btnViewOnly"
        android:background="#159F85"
        android:padding="5dp"
        android:text="Download"
        android:textColor="@color/white" />

    <Button
        android:id="@+id/btnViewOnly"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="31dp"
        android:background="#159F85"
        android:text="View"
        android:textColor="@color/white" />

</RelativeLayout>